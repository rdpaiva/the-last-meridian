{
  "name": "@space-duel/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --banner:js=\"import { createRequire as __createRequire } from 'node:module'; const require = __createRequire(import.meta.url);\" --external:bufferutil --external:utf-8-validate --outfile=dist/server.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^7.50.0",
    "@colyseus/schema": "^4.0.26",
    "@space-duel/shared": "*",
    "colyseus": "^0.17.10"
  },
  "devDependencies": {
    "@colyseus/testing": "^0.17.11",
    "esbuild": "^0.28.1",
    "tsx": "^4.22.4"
  }
}
